{
    "name": "@pulse/backend",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "$npm_execpath run build && vite preview",
        "deploy": "$npm_execpath run build && wrangler deploy",
        "db:create": "wrangler d1 create DB",
        "db:drop:local": "wrangler d1 execute DB --local --command=\"DROP TABLE IF EXISTS events; DROP TABLE IF EXISTS daily_stats; DROP TABLE IF EXISTS daily_pages; DROP TABLE IF EXISTS daily_referrers; DROP TABLE IF EXISTS daily_countries; DROP TABLE IF EXISTS daily_browsers; DROP TABLE IF EXISTS daily_devices; DROP TABLE IF EXISTS projects; DROP TABLE IF EXISTS refresh_tokens; DROP TABLE IF EXISTS reset_tokens; DROP TABLE IF EXISTS users; DROP TABLE IF EXISTS d1_migrations;\"",
        "db:migrate:local": "wrangler d1 migrations apply DB --local",
        "db:migrate:remote": "wrangler d1 migrations apply DB --remote",
        "db:seed:local": "npx tsx src/scripts/seed.ts && wrangler d1 execute DB --local --file=.wrangler/seed/seed.sql",
        "db:fresh:local": "bun db:drop:local && bun db:migrate:local && bun db:seed:local",
        "dev:cf": "wrangler dev",
        "dev:full": "bun db:migrate:local && bun dev:cf",
        "cf-typegen": "wrangler types --env-interface CloudflareBindings"
    },
    "dependencies": {
        "@hono/zod-validator": "^0.7.6",
        "hono": "4.12.9",
        "resend": "^6.9.3",
        "ua-parser-js": "^2.0.9",
        "zod": "^4.2.1"
    },
    "devDependencies": {
        "@cloudflare/vite-plugin": "^1.2.3",
        "vite": "^6.3.5",
        "vite-ssr-components": "^0.5.1",
        "wrangler": "^4.17.0"
    },
    "exports": {
        ".": "./src/index.ts"
    }
}
